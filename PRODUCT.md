# Pijoo

## 产品定义

Pijoo 使用同一种频道模型承载所有对话：账号默认拥有一个以用户昵称命名的助理频道，只有两名成员的
频道显示为“好友”。用户可以直接在 App 中和助理或好友聊天；连接的 Codex 会话负责处理消息，
并可安全使用用户授权的历史。

## 用户问题

- 用户的背景、偏好和既往决策散落在多个 Codex 任务里，每次都要重新说明；
- 外部真人或 AI 可以发来消息，但用户不应为每条消息手动选择应进入哪个 Codex 任务；
- 自动读取全部历史或自动代表用户回复，会越过隐私、身份和授权边界。

## 首版体验

```text
登录 Pijoo
  -> 自动进入以自己昵称命名的默认助理频道
  -> 在 ~/Pijoo 自动新建或连接一个已有 Codex 会话
  -> 直接在 App 中和助理聊天
  -> 用户选择允许读取的既有 Codex 任务
  -> 创建或加入一个双人频道，列表显示为“好友”
  -> 好友消息按该频道已有设置进入助理会话
  -> 助理生成带来源的回复草稿
  -> 用户明确确认后发送
```

用户只需要理解自己的助理、好友和群聊。Channel 是统一数据模型；TaskBinding、Subscription、
endpoint 和游标仍是内部实现。

## P0 范围

- 每个本机账号自动拥有一个以当前昵称显示的默认助理频道，并只连接一个助理会话；
- App 自动创建 `~/Pijoo`，新建专属会话默认使用该工作目录；
- 双人频道在 App 中显示为“好友”，标题优先使用对方昵称；
- 用户逐项授权或撤销 Codex 历史任务，默认无授权；
- 助理只读检索获授权任务，返回有来源的有限片段，不修改原任务；
- 用户可要求助理基于授权历史整理一份可查看、可纠正、可删除的个人画像草稿；
- 首版只连接一个好友，使用一次性邀请；
- App、助理会话和好友继续通过同一套频道收发、恢复与回执链路交流；
- 回复默认为草稿，只有用户明确确认后才发送；
- 复用现有账号、Membership、消息、SSE、回执、恢复和 Host Connector。

## P0 不包含

- 默认读取全部 Codex 历史或直接依赖 Codex 内部生成的 memory 文件；
- 自动回复、代表用户承诺、自动执行文件、Shell、浏览器、部署或付款；
- 多助理、公开联系人目录、群聊产品化或第二个 Host；
- 把完整任务历史、工作目录或个人画像上传到 Channel Service；
- 宣称端到端加密。当前服务端可见消息明文，个人敏感上下文不得经消息中继传输。

## 安全原则

1. **默认无权**：历史读取按 task allowlist 授权，可随时撤销。
2. **本地读取**：历史正文只在本机通过 Codex 支持的接口读取；服务端只接触必要的外部消息。
3. **来源可见**：画像结论、历史回答和外部消息都能追溯到来源 task 或联系人。
4. **草稿优先**：读取上下文不等于获得发送或执行权限。
5. **数据最少**：只向助理任务提供回答当前问题所需的有限片段。
6. **不猜路由**：外部消息只进入用户明确指定的助理任务，不按最近活跃任务猜测。
7. **能力分层**：已接收、已投递、已生成草稿、用户已确认和已发送是不同状态。

## P0 完成标准

1. 新用户登录后自动获得昵称命名的默认助理频道，连接一个 Codex 会话后可直接双向聊天；
2. 未授权 task 的标题与正文均不会出现在检索结果中；
3. 授权、检索、撤销在真实 Codex 任务上生效，结果包含可核对来源；
4. 画像草稿可查看来源、纠正和删除，且不自动作为对外身份声明；
5. 双人频道显示为“好友”和对方昵称，消息只进入该频道连接的会话；
6. 未经用户明确确认不会向联系人发送消息；
7. 撤销历史授权或联系人后，旧权限立即不可再用；
8. App 重启和短暂断线不静默丢消息，不把未知结果自动重试为重复发送；
9. 自动化通过后，再完成真实账号、真实 Codex 和真实联系人的端到端验收。
