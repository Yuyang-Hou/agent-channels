# Verification

Status: release preparation in progress.

## Expected Evidence

- Exact pushed `main` commit.
- `Pijoo-0.3.0-beta.19-arm64.dmg` size and SHA-256.
- Developer ID identities for App, sidecar, updater and DMG.
- Mounted App release version `0.3.0-beta.19`, build `19`, MCP `serverInfo.version` and bundled Skill checks.
- Explicit notarization, installation and GitHub Release boundary.
